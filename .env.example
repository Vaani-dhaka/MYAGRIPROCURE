# Port for the Express + Vite development server.
PORT=3000

# Secret used to sign the local session token.
JWT_SECRET="replace-with-a-long-random-secret"

# Optional URL for hosted environments.
APP_URL="http://localhost:3000"
